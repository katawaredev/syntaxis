[package]
name = "syntaxis-git"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
async-trait = "0.1.89"
serde = { version = "1.0.219", features = ["derive"] }
syntaxis-workspace = { path = "../workspace" }

[lints]
workspace = true
