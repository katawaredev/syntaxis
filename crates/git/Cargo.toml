[package]
name = "syntaxis-git"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
async-trait = "0.1.92"
serde = { version = "1.0.229", features = ["derive"] }
syntaxis-workspace = { path = "../workspace" }

[lints]
workspace = true
